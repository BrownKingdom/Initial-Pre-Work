<!-- Returning a true false value with the comparison operators to produce boolean values. -->
function isLess(a, b) {
  // Fix this code
  return (a < b) ;
}

// Change these values to test
isLess(10, 15);
