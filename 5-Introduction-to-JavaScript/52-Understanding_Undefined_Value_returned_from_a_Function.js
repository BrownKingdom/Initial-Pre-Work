<!-- A function does not have to include a return, the function processes the inner code as undefined. Remember to place a numeral in the sum equation. -->
// Example
var sum = 0;
function addThree() {
  sum = sum + 3;
}

// Only change code below this line
var sum = 8;
function addFive() {
  sum = sum + 5;
}
var returnedValue = addFive(5); 

// Only change code above this line
var returnedValue = addFive();
