<!-- math random generates Randomized numbers are useful for creating randomized behaviors with decimals. -->
function randomFraction() {

  // Only change code below this line.

  return Math.random ();

  // Only change code above this line.
}
