<!-- Compare a value the logical and operator to determine if the operation order is true left to right. -->
function testLogicalAnd(val) {
  // Only change code below this line

  if (val <= 50 && val >= 25) {
      return "Yes";
    }
  

  // Only change code above this line
  return "No";
}

// Change this value to test
testLogicalAnd(30);
