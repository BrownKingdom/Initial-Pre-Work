<!-- Use the strict equality triple equal in the if statement so the function will render a equal when the numerical val is strictly equal to that numerical value. -->
// Setup
function testStrict(val) {
  if (val === 7) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testStrict(10);
